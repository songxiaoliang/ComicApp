import {
    StyleSheet
} from 'react-native';

export let loadingMoreFooterStyle = StyleSheet.create({

    container: {
        height: 40,
        flexDirection: 'row',
        justifyContent: 'center',
        alignItems: 'center'
    },

    title: {
        fontSize: 14,
        color: 'gray',
        marginLeft: 10
    }
})